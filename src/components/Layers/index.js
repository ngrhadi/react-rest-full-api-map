import React from 'react';
import Api from '../../api/api';

const Layers = () => {
    return (
        <div>
            Layer Here
            <Api/>
        </div>
    );
}

export default Layers;
